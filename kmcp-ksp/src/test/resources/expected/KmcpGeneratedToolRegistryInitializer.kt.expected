package sh.ondr.kmcp.generated

import sh.ondr.kmcp.runtime.KMCP

object KmcpGeneratedToolRegistryInitializer {
    init {
        KMCP.toolInfos["greetUser"] = ToolInfo(...)
        KMCP.toolHandlers["greetUser"] = ToolHandler(...)
        KMCP.toolInfos["myTool"] = ToolInfo(...)
        KMCP.toolHandlers["myTool"] = ToolHandler(...)
    }
}
